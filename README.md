# React - React-native code sharing

Share code between your react and react-native app

## Install

Run `npm install`
for web run `npm run dev`

for mobile `npm start`
and `react-native run-android` for android
